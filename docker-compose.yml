services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: botfinancas
      POSTGRES_PASSWORD: botfinancas
      POSTGRES_DB: botfinancas
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U botfinancas"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
